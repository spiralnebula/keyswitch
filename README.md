Key Switch
==========

*Replaces you radio buttons.*

 - Allows input fields to popup on specified options.

```javascript
keyswtich.make({
    class_name : {}
    with       : {
        option : {
            choice        : Array,
            default_value : String
        },
        input : Object
    }
})
```